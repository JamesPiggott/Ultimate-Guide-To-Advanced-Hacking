# host

Translate hostnames into IP address. Host is usually installed on most Linux systems. It will return Ipv4 and Ipv6 addresses as well as name servers. Using the IP to obtain hostname is also possible.

Examples:

host -a [target_url]

    -a: verbose output
    

