# traceroute

A well-know application to map the route your packets will take to a host. It uses the TTL field of packets to force servers to identify themselves.

Examples:

traceroute -e [target_url]

    -e: firewall evasion